import {View} from './index';

export default class Scene {
    constructor(model,) {
        this.stage = null;
        this.el = el;
    }
    render() {
        console.log('scene render');
    }

}